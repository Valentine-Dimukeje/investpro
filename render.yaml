services:
  - type: web
    name: octa-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn investpro_backend.wsgi:application
    autoDeploy: true
